class Solution:
    def rearrangeArray(self, nums: List[int]) -> List[int]:
        nums.sort()
        size = len(nums)
        i=1
        while i <=size-1:
            nums[i-1], nums[i] = nums[i], nums[i-1]
            i+=2
       
            
        return nums
